import core


# todo: display the json stuff, haven't think of what formation the json is
def display_json(json):

def main():
    while 1:
        word = input('Please type the word/phrase you want to look up:')
        if word[-3:0] is not 'fuck':
            print(display_json(core.lookup(word)))

