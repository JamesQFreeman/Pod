from distutils.core import setup
setup(
  name = 'podict',
  packages = ['podict'], # this must be the same as the name above
  version = '0.1',
  description = 'A command-line version Oxford Dictionary',
  author = 'Sheng Wang',
  author_email = 'wang_s1998@163.com',
  url = 'https://github.com/peterldowns/mypackage', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['Dictionary'], # arbitrary keywords
  classifiers = [],
)